title: "A `file*@` slot with a pinned value to a present file resolves cleanly"
spec_ref: "[[type-def shape suffixes::au-type-system]], [[spec - pinned references - a recorded resolved edge with an immutable past and an on-demand forward trace]]"
command: validate
expected:
  clean: true
