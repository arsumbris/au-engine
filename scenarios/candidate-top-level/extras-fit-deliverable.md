---
type: note
description: "A migration plan"
audience: external
---

# A note with deliverable-shaped extras

The instance claims `note`, supplying `description`. The extra `audience: external` happens to match `deliverable`'s required field (along with description). Engine surfaces `deliverable` and `summary` as implicit identity candidates — the file *could* claim them.
