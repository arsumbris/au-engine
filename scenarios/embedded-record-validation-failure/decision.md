---
type: decision
description: "Migrate dev DB to SQLite"
assumptions:
---

# Assumptions

```yaml [:assumptions]
type: assumption
confidence: high
```

The block above omits `description`, which `assumption` requires.
