---
type: measurement
length:
---

The span is `[:length] meter(42)`. The written brand `meter` picks the member,
where a bare `42` would be `brand-constructor-required` (indistinguishable from
`second`).
