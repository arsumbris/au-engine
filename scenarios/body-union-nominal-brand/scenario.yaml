title: "Body union-of-nominals — `meter(42)` at a `<meter | second>` slot, contributed from the body, discriminates by the written brand like the frontmatter case"
spec_ref: "[[spec - value model - one typed value produced once in effective_values and consumed by validation and reads]]"
command: validate
expected:
  clean: true
