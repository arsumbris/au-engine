---
type: decision
description: "Migrate dev DB to SQLite"
assumptions:
---

# Why

Carries forward the assumption from [[research-notes^^extractor-stability:assumptions]].
The block's `type:` is `assumption`, which satisfies the `assumption&[+]` slot —
the host file's claim (`note`) is irrelevant for shape purposes when a block-id
is present.
