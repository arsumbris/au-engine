title: "A self-`::repo` qualifier (`note::app` inside `app`) carries the `type-repo-self` hint and still validates against the own type, so a missing required field fires"
spec_ref: "[[design - cross-repo type vocabulary - reference import and vendor as one spectrum over the repo qualifier]]"
command: validate
expected:
  codes:
    - { code: type-repo-self, severity: hint }
    - { code: required-field-absent, severity: error }
    - { code: repo-folder-name-mismatch, severity: drift }
