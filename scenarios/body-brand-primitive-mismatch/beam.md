---
type: measurement
length:
---

The span is `[:length] Number(5)`, a reserved primitive is admitted only as a
union member, so `Number(...)` at a single `meter` slot is a mismatch. Its inner
`5` would coerce to a valid metre, so before the value model carried the written
brand faithfully the body path accepted it silently.
