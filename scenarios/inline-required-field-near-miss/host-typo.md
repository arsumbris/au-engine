---
# The inline `detail` record omits required `description`; `descriptn` is a
# near-miss of it, so required-field-absent carries a "did you mean" hint.
type: host
detail:
  descriptn: "typo of description"
---
