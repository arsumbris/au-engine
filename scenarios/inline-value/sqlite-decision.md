---
type: decision-with-rationale
summary: "Migrate to SQLite"
rationale:
  description: "Performance gains and operational simplicity."
---

# A decision with an inline rationale

`rationale` is declared as a record-typed slot. The value is a YAML map mirroring rationale's frontmatter — `type:` is omitted and defaults to the slot's demand.
