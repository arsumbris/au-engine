---
type:
  - note
  - deliverable
description: "Migration plan"
audience: external
---

# A mixin instance

This file IS-A `note` AND a `deliverable`. Both declare `description: String` (token-equal),
so it auto-unifies — one entry in the effective shape with `origins: [note, deliverable]`.
