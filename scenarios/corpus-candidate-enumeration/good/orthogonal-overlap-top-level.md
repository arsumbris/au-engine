---
type: note
description: "An idea sketch"
---

Claims `note`. `summary` requires the same `description` — surfaces as a top-level candidate.
This is the orthogonal-overlap case that the spec amendment specifically calls out.
