---
type: note
description: "Q3 roadmap draft"
audience: internal
---

`audience` is an extra under the `note` claim. `deliverable` requires both
`description` and `audience` — both present — so it surfaces with a larger
required-set than `summary` and outranks it.
