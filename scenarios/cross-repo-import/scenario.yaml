title: "A `type: foo::repo` import resolves against the folded peer type: a claim and a subtype both enforce the peer's required field across the repo boundary, supplying it validates cleanly"
spec_ref: "[[design - cross-repo type vocabulary - reference import and vendor as one spectrum over the repo qualifier]]"
command: validate
expected:
  codes:
    - { code: required-field-absent, severity: error }
