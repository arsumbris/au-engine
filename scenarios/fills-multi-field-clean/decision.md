---
type: decision
description: "Migrate dev DB to SQLite"
causes:
effects:
---

# Reasoning

Because `[:causes] shared-volume contention dominated`, we observe
`[:effects] dev pipelines run cleanly`.
