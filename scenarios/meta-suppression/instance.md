---
type: suppressed
description: "The suppressed type-def cuts the meta walk for display-meta."
---

# Suppression demo

The introspect JSON doesn't currently expose meta blocks or walk results, so the suppression semantics aren't observable from the UI — only the load is clean. See `memory/gap - 2605110100 - meta blocks not on the json wire.md`.
