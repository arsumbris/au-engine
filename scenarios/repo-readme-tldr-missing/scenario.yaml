title: "A root README with no tldr field fires required-field-absent"
spec_ref: "[[spec - repo readme - an engine-owned self-declaring node at the repo root with a required body template]]"
command: validate
expected:
  codes:
    - { code: required-field-absent, severity: error }
