---
type: decision
description: "Migrate dev DB to SQLite"
assumptions:
---

# Why

Carries forward [[research-notes^passage:assumptions]] as evidence.
The bare `^passage` is navigational, so the FILE `research-notes` is the
contributed value — it is an `assumption`, which satisfies `assumption&[+]` —
and `^passage` is a jump anchor that resolves. Clean.
