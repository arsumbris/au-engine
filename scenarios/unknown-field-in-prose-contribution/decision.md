---
type: decision
description: "Migrate dev DB to SQLite"
---

Decided on `[:decided_at] 2026-04-15` — but `decided_at` is not a
field of `decision`'s closure, so the prose contribution targets an
unknown field (advisory).
