title: "Body union with a tuple-brand member — `point(1, 2)` at a `<point | second>` slot, contributed from the body, validates positionally like the frontmatter case"
spec_ref: "[[spec - value model - one typed value produced once in effective_values and consumed by validation and reads]]"
command: validate
expected:
  clean: true
