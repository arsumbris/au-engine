---
type: note
description: "Notes file with an untyped block-id (no typed fence preceding the marker)"
---

# Findings

A plain paragraph that ends here.
^untyped-block

The marker above sits on its own line after ordinary prose, so it
resolves to a BlockIdMarker event without a typed yaml fence.
