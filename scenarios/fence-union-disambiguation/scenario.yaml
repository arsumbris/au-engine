title: "At a union slot the fence content picks the branch: a typed mapping reads as a record, anything else as text"
spec_ref: "[[type-instance body contribution::au-type-system]], [[type-def shape record::au-type-system]]"
command: validate
expected:
  clean: true
