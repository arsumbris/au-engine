---
type: reading
title: "On fences"
pages:
---

# Pages

`[:pages] not a number at all`

The value arrives only from the body, so the frontmatter pass never sees it.
It is still checked against `Number`.
