---
type: note
description: "A simple note."
---

# Nothing to suggest

The instance claims `note`, supplies its required `description`, and carries no extras. There's no other type-def whose required-field set is satisfied, so the candidate scan returns nothing.
