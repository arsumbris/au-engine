title: "A repo.yaml written as `au.engine.workspace` omits its floor, a wrong-kind error"
spec_ref: "[[spec - engine-schema file claims - the kind assigns a floor, a written type self-describes and mixes in more]]"
command: validate
expected:
  codes:
    - { code: engine-schema-type-floor-omitted, severity: error }
