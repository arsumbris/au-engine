---
type: citation
source: "https://arxiv.org/abs/2401.12345"
---

# Url branch — `^https?://` prefix matches

`<String | Url>` is disambiguated by the `^https?://` prefix (parallel to the wikilink discriminator). This value resolves as `Url`.
