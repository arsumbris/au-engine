---
type: citation
source: "Bowman & Williamson, 2024, Proc. of XYZ"
---

# String branch — no `^https?://` prefix

Non-matching strings fall through to the `String` branch. Both branches share the same slot, no `type:` discriminator is needed — the prefix predicate decides.
