---
type: note::base
---

An instance in `app` claiming `base`'s `note`, whose `location` demands `notes/`.
Placed at the app root, so the peer type's location is unmet, checked relative to
app's own root.
