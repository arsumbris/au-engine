---
type: decision
description: "Migrate dev DB to SQLite"
assumptions:
rationale:
---

# Assumptions

`[:assumptions] Page numbering is monotonic.` and also
`[:rationale] SQLite scales fine at dev volume.` — the rationale
contribution violates the section's exclusivity.
