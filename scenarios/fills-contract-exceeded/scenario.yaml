title: "Section `fills!: f` with a contribution to a different field fires fills-contract-exceeded"
spec_ref: "[[type-def body fills::au-type-system]]"
command: validate
expected:
  codes:
    - { code: fills-contract-exceeded, severity: error }
