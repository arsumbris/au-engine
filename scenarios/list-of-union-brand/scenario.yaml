title: "List of a union brand — a `quantity[]` slot resolves each element by its written brand, `meter(...)` and `second(...)` pick their member per element"
spec_ref: "[[spec - value model - one typed value produced once in effective_values and consumed by validation and reads]]"
command: validate
expected:
  clean: true
