title: "A body fence contributing to an `opaque` field is uninterpreted — captured verbatim, never parsed as a record"
spec_ref: "[[type-def shape opaque::au-type-system]], [[type-instance body contribution::au-type-system]]"
command: validate
expected:
  clean: true
