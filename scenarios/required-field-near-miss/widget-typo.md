---
# `siize` is a typo of the required `size`, so required-field-absent fires with
# a "did you mean 'siize'?" near-miss hint and a related span at the typo'd key.
# `siize` itself is a legal open-world extra, so it carries no diagnostic.
type: widget
siize: 7
---
