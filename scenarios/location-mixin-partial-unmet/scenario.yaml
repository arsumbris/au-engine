title: "A raw mixin matching one claimed soft location but not the other is a location-partial-unmet hint"
spec_ref: "[[spec - location constraints - a name template and path predicate as an advisory placement meet]]"
command: validate
expected:
  codes:
    - { code: location-partial-unmet, severity: hint }
