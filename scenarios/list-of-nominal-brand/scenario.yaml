title: "List of a nominal brand — a `meter[]` slot resolves each element through the value model, a bare value coerces and a `meter(...)` constructor validates, per element"
spec_ref: "[[spec - value model - one typed value produced once in effective_values and consumed by validation and reads]]"
command: validate
expected:
  clean: true
