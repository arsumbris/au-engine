---
type:
  - note
  - tagged
description: "A file claiming both note and tagged."
tag: "alpha"
---

# A mixin target

Mixin claim of `note` AND `tagged`. Satisfies the `<note* & tagged*>` constraint by being both at once.
