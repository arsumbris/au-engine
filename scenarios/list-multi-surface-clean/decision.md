---
type: decision
description: "Migrate dev DB to SQLite"
sources:
  - "[[paper-a]]"
---

# Sources

See [[paper-b:sources]] for the second source.

```yaml [:sources]
type: source
description: "Inline source, contributed via yaml block"
```

Three distinct values: one via frontmatter, one via body.wikilink, one
via body.yaml-block. The list accumulates three ValueContainers.
